# PluginJobRunner
Example project using (OpenApiWithApiKeyTemplate)[https://github.com/larsbuch/OpenApiWithApiKeyTemplate] as a template and internally making use of (RuntimePluggableClassFactory)[https://github.com/DevelApp-dk/RuntimePluggableClassFactory] to make a task runner where you can plugin tasks easily
